public class equalsMethod {
    /**
     * FOR A DICE CLASS
     * @Override
     * public boolean equals(Object other){
     *     if (other == null) {
     *         return false;
     *     } else if (other == this) {
     *         return true;
     *     } else if (!(other instanceOf Dice)) {
     *         return false;
     *     }
     *
     *     Dice otherDice = (Dice) other;
     *
     *     return other.currentValue == this.currentValue && other.numSides == this.numSides
     * }
     */
}
